# 智能相册回忆录 PRD（Web + App 双端上线版）v3

更新日期：2026-03-31  
产品形态：Web（H5/PC浏览器）+ App（iOS/Android）  

## 1. 背景与问题
用户相册内容增长迅速，重要回忆被淹没。用户希望能够在低门槛的操作下，从相册中选取素材并一键生成带音乐、叙述、转场的回忆录视频，并能保存/分享。

受浏览器安全模型限制，Web 端无法像 App 一样“授权相册后遍历全库并做后台索引”。因此本版本采用双端分层：
- Web：以“手动选择上传素材”为主流程，提供最小完整体验闭环。
- App：提供完整能力（相册授权、全库/增量索引、智能筛选、人物识别、稳定视频生成与写回相册）。

## 2. 目标与非目标

### 2.1 目标（Goals）
- Web 版本实现从“手动选择素材 → 预览优化 → 选择风格 → 生成 → 下载/分享”的完整闭环。
- App 版本实现“相册授权 → 智能筛选/手动选择 → 预览优化 → 风格 → 生成 → 写回相册/分享”的完整闭环。
- 统一的产品心智：素材选取（两条路）+ 预览优化（去重/质量）+ 风格化生成（音乐/叙述/转场）+ 输出（保存/分享）。

### 2.2 非目标（Non-goals）
- Web 端不做全库相册访问、后台索引、人物识别、系统相册写回。
- Web 端不保证在所有内置浏览器（如微信内置 WebView）实现本地视频编码能力；提供降级方案。

## 3. 用户与场景

### 3.1 目标用户
- 轻度用户：偶尔做纪念视频，偏好简单操作（情侣/亲子/旅行）。
- 重度用户：素材量大，希望通过智能筛选快速定位素材。

### 3.2 核心场景
1) 手动挑选本次想用的照片/视频 → 生成回忆录 → 分享到社交平台  
2)（App）授权相册后，选择时间/人物/格式 → 自动筛选 → 生成回忆录  

## 4. 双端能力边界与产品策略

### 4.1 Web 版本（可上线范围）
**素材获取方式：仅手动选择/上传**
- 使用文件选择器选择图片/视频（可多选），作为本次回忆录素材库。
- 不支持：相册授权全库遍历、后台索引、人物识别。

**智能筛选入口策略**
- Web 仍展示“智能筛选”入口，但点击后弹出“下载 App 体验智能筛选”的提示（详见 6.2）。

**生成能力策略**
- 优先：在支持的浏览器（如 Android Chrome、桌面 Chrome）采用浏览器端生成（Canvas + MediaRecorder）。
- 降级：在不支持的浏览器（如微信内置）使用服务端生成（上传用户选择的素材，服务端合成 mp4）或直接提示“请使用 App”。

### 4.2 App 版本（完整能力）
- 相册权限授权（iOS Photos / Android MediaStore）。
- 全库与增量媒体读取；必要的缩略图与原图获取。
- 智能筛选：时间/人物/格式维度筛选。
- 人物识别：目标人物建档 + 素材匹配（端侧优先）。
- 视频稳定生成：端侧/本地服务生成 mp4，支持写回系统相册、分享。

## 5. 信息架构与流程

### 5.1 页面结构（建议）
1) 启动页/引导页  
2) 权限页（仅 App）  
3) 选择素材页（两条模式）
   - 手动选择（Web/App）
   - 智能筛选（App；Web 点击提示下载 App）
4) 素材预览页（去重/质量/顺序调整）
5) 风格选择页（模板、文字语气、音乐预设）
6) 生成页（进度、预览、下载/保存/分享）
7) 历史页（回忆录列表、再次分享/再次编辑）

## 6. 功能需求（按模块）

### 6.1 素材选取（手动选择：Web/App）
**Web**
- 入口：选择方式 → 手动选择
- 能力：
  - 多选导入图片/视频（系统文件选择器）。
  - “相册列表/最近/收藏”在 Web 端为“导入素材内的视图组织”（非系统相册），可用简单逻辑实现：
    - 最近：按导入时间或文件时间排序；
    - 收藏：用户在网格里点星标；
    - 相册列表：以“导入批次/用户自定义分组”模拟。
- 约束提示：大文件/大量文件时提示“建议分批导入”。

**App**
- 入口：选择方式 → 手动选择
- 能力：浏览系统相册、相册列表/最近/收藏、批量勾选素材。

### 6.2 智能筛选入口（Web 与 App 分流）
**Web：点击智能筛选时弹窗提示下载 App**
- 触发：用户点击“智能筛选”卡片/按钮
- 行为：弹窗（不可跳过/可关闭）
  - 标题：在 App 里体验智能筛选
  - 文案：Web 版暂不支持相册授权与全库筛选。下载 App 后可按时间/人物/格式一键筛选并生成回忆录。
  - CTA：
    - 主按钮：下载 App（跳转 App Store / 应用商店 / 下载页）
    - 次按钮：继续使用手动选择

**App：智能筛选真实可用**
- 时间维度：近一周/近一月/近三月/近一年/自定义/特殊日期（生日/节日/纪念日）。
- 人物维度：选择已有相册人物或上传目标人物照片建档。
- 格式维度：照片/视频/Live Photo/截图（默认排除截图）。

### 6.3 素材预览与优化（Web/App）
- 去重：
  - 自动建议相似组，默认保留“质量评分最高”的一张；
  - 支持用户在相似组中切换保留项。
- 质量筛选：
  - 开关：过滤低质量；
  - 阈值：滑杆（0.2~0.9）。
- 顺序调整：
  - 支持上移/下移；
  - 后续生成以当前顺序为准。

### 6.4 风格选择（Web/App）
- 模板：免费/付费模板分区（付费模板提示购买）。
- 文字语气：温情/文艺/幽默/简约（影响叙述文案生成）。
- 音乐预设：温情/甜蜜/旅行/怀旧（影响背景音乐选择或合成）。

### 6.5 文案生成（Web/App）
目标：生成“优美叙述性文字”，且能根据素材信息动态变化。
- 输入：
  - 用户选择的模板/语气
  - 素材元信息：拍摄时间、地点、（App）人物标签
- 输出：
  - 开场标题（可编辑）
  - 分段旁白（按素材或按时间段聚合，至少 3~8 句）
  - 结尾寄语（可编辑）
- 交互：
  - 一键重新生成（不改变素材）
  - 用户可手动修改任何文案字段

### 6.6 获取拍摄时间/地点（Web/App）
**Web**
- 可获取：
  - 文件选择器提供的文件时间（lastModified，作为兜底）
  - 部分 JPEG 图片可读取 EXIF（DateTimeOriginal、GPS）作为增强
- 不保证：
  - HEIC/部分 PNG/WebP 的 EXIF
  - 精确地点名称（浏览器端默认不做逆地理编码；可展示经纬度）

**App**
- 优先从系统相册资产元数据获取（拍摄时间、经纬度、地点名缓存等）。

### 6.7 视频生成与转场（Web/App）
**Web**
- 推荐策略：
  - 可用环境：浏览器本地合成（Canvas 渲染 + MediaRecorder 录制）
  - 不可用环境（微信内置）：服务端合成或提示使用 App
- 转场要求（MVP）：
  - Ken Burns（轻缩放/平移）
  - Crossfade
  - Slide-in（轻微）

**App**
- 端侧稳定合成 mp4，支持更丰富的转场与特效。

### 6.8 音乐（Web/App）
**Web**
- 浏览器端生成：使用 WebAudio 合成或选择内置音频（注意自动播放限制）。
- 服务端生成：由服务端合成音轨（或贴片音频）。

**App**
- 选择曲库音乐、淡入淡出、音量、（后续）按节奏卡点。

### 6.9 输出：下载/保存/分享（Web/App）
**Web**
- 下载视频文件（mp4/webm 视环境而定）。
- 调用 Web Share（支持时）分享文件。

**App**
- 保存到系统相册（写回）
- 系统分享面板

### 6.10 历史记录（Web/App）
- 本地保存历史条目（封面、标题、风格、创建时间、素材数量）。
- 支持再次播放、再次分享、再次生成。

## 7. 异常与降级策略
- 微信内置浏览器：
  - 本地生成不可用时，显示明确原因与“服务端生成”按钮；
  - 无法连到服务端时提示：检查电脑端服务是否启动、是否同一 Wi‑Fi、是否被防火墙拦截。
- 素材为空：禁止下一步。
- 大批量素材：提示分批导入/降低分辨率（720P）。
- 元信息缺失：时间地点展示为“未知”，文案自动回退为通用叙述。

## 8. 数据与隐私合规
- Web：
  - 仅处理用户手动选择的文件；
  - 若启用服务端生成，需在 UI 显示“将上传所选素材到局域网电脑进行合成”的提示，并提供退出/取消。
- App：
  - 明确权限用途：仅用于生成回忆录；
  - 提供“清除索引/清除缓存/撤销授权”入口；
  - 端侧优先，不上传原图；如上传需用户明确授权与加密传输。

## 9. 埋点（建议）
- 模式选择：手动/智能点击次数（Web 智能点击→下载引导转化）。
- 导入素材：数量、类型、失败率。
- 预览优化：去重开关、质量阈值、顺序调整次数。
- 生成成功率：按浏览器环境（微信/Chrome/Safari），按路径（本地/服务端）。
- 下载/分享：成功率与渠道。

## 10. 版本规划（建议）
- Web v1：手动选择 + 预览优化 + 风格 + 本地生成（可用环境）+ 下载/分享 + 微信兜底服务端生成。
- App v1：相册授权 + 手动选择 + 智能筛选（不含人物识别）+ 稳定生成 + 写回相册。
- App v2：人物识别 + 增量索引 + 更丰富转场/特效 + 曲库音乐与卡点。

